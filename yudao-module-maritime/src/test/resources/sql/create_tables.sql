CREATE TABLE IF NOT EXISTS `maritime_course_session` (
    `id` bigint NOT NULL GENERATED BY DEFAULT AS IDENTITY COMMENT '班期ID',
    `course_id` bigint NOT NULL COMMENT '课程ID',
    `session_code` varchar(30) NOT NULL COMMENT '班期编号',
    `session_name` varchar(50) DEFAULT NULL COMMENT '班级名称',
    `location` varchar(100) NOT NULL COMMENT '上课地点',
    `duration_days` int NOT NULL COMMENT '课程天数',
    `instructor_info` varchar(500) DEFAULT NULL COMMENT '讲师信息',
    `start_date` date NOT NULL COMMENT '开班日期',
    `end_date` date NOT NULL COMMENT '结束日期',
    `enrollment_deadline` date DEFAULT NULL COMMENT '报名截止日期',
    `max_students` int NOT NULL DEFAULT 30 COMMENT '最大招生人数',
    `enrolled_count` int NOT NULL DEFAULT 0 COMMENT '已报名人数',
    `session_status` varchar(20) NOT NULL DEFAULT 'DRAFT' COMMENT '班期状态',
    `remark` text DEFAULT NULL COMMENT '备注（富文本HTML）',
    `creator` varchar(64) DEFAULT '',
    `create_time` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP,
    `updater` varchar(64) DEFAULT '',
    `update_time` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP,
    `deleted` bit NOT NULL DEFAULT FALSE,
    `tenant_id` bigint NOT NULL DEFAULT 0,
    PRIMARY KEY (`id`)
) COMMENT '班期表';

CREATE TABLE IF NOT EXISTS `maritime_session_fee_config` (
    `id` bigint NOT NULL GENERATED BY DEFAULT AS IDENTITY COMMENT '费用配置ID',
    `session_id` bigint NOT NULL COMMENT '班期ID',
    `tuition_amount` decimal(10,2) NOT NULL COMMENT '学费总额',
    `tuition_description` text DEFAULT NULL COMMENT '学费说明JSON',
    `deposit_amount` decimal(10,2) NOT NULL COMMENT '定金金额',
    `is_groupon_enabled` tinyint NOT NULL DEFAULT 0 COMMENT '是否开启拼团',
    `groupon_discount_amount` decimal(10,2) NOT NULL DEFAULT 0 COMMENT '拼团优惠减免',
    `groupon_required_count` int NOT NULL DEFAULT 3 COMMENT '拼团所需人数',
    `groupon_expire_hours` int NOT NULL DEFAULT 24 COMMENT '拼团有效时间',
    `groupon_fail_discount_amount` decimal(10,2) NOT NULL DEFAULT 0 COMMENT '降级优惠',
    `referral_commission_amount` decimal(10,2) NOT NULL DEFAULT 0 COMMENT '推荐佣金',
    `is_referral_commission_enabled` tinyint NOT NULL DEFAULT 1 COMMENT '是否开启佣金',
    `is_deposit_refundable` tinyint NOT NULL DEFAULT 1 COMMENT '定金是否可退',
    `balance_due_days_before_start` int NOT NULL DEFAULT 7 COMMENT '尾款截止天数',
    `refund_policy_text` text DEFAULT NULL COMMENT '退款政策',
    `creator` varchar(64) DEFAULT '',
    `create_time` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP,
    `updater` varchar(64) DEFAULT '',
    `update_time` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP,
    `deleted` bit NOT NULL DEFAULT FALSE,
    `tenant_id` bigint NOT NULL DEFAULT 0,
    PRIMARY KEY (`id`)
) COMMENT '班期费用配置表';

CREATE TABLE IF NOT EXISTS `maritime_fee_template` (
    `id` bigint NOT NULL GENERATED BY DEFAULT AS IDENTITY COMMENT '模板ID',
    `name` varchar(50) NOT NULL COMMENT '模板名称',
    `tuition_amount` decimal(10,2) NOT NULL COMMENT '学费总额',
    `tuition_description` text DEFAULT NULL COMMENT '学费说明JSON',
    `deposit_amount` decimal(10,2) NOT NULL COMMENT '定金金额',
    `is_groupon_enabled` tinyint NOT NULL DEFAULT 0 COMMENT '是否开启拼团',
    `groupon_discount_amount` decimal(10,2) NOT NULL DEFAULT 0 COMMENT '拼团优惠减免',
    `groupon_required_count` int NOT NULL DEFAULT 3 COMMENT '拼团所需人数',
    `groupon_expire_hours` int NOT NULL DEFAULT 24 COMMENT '拼团有效时间',
    `groupon_fail_discount_amount` decimal(10,2) NOT NULL DEFAULT 0 COMMENT '降级优惠',
    `referral_commission_amount` decimal(10,2) NOT NULL DEFAULT 0 COMMENT '推荐佣金',
    `is_referral_commission_enabled` tinyint NOT NULL DEFAULT 1 COMMENT '是否开启佣金',
    `is_deposit_refundable` tinyint NOT NULL DEFAULT 1 COMMENT '定金是否可退',
    `balance_due_days_before_start` int NOT NULL DEFAULT 7 COMMENT '尾款截止天数',
    `refund_policy_text` text DEFAULT NULL COMMENT '退款政策',
    `status` tinyint NOT NULL DEFAULT 1 COMMENT '状态',
    `creator` varchar(64) DEFAULT '',
    `create_time` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP,
    `updater` varchar(64) DEFAULT '',
    `update_time` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP,
    `deleted` bit NOT NULL DEFAULT FALSE,
    `tenant_id` bigint NOT NULL DEFAULT 0,
    PRIMARY KEY (`id`)
) COMMENT '费用模板表';

CREATE TABLE IF NOT EXISTS `maritime_enrollment` (
    `id` bigint NOT NULL GENERATED BY DEFAULT AS IDENTITY COMMENT '报名ID',
    `enrollment_no` varchar(64) NOT NULL COMMENT '报名单号',
    `member_id` bigint NOT NULL COMMENT '学员ID',
    `session_id` bigint NOT NULL COMMENT '班期ID',
    `groupon_record_id` bigint DEFAULT NULL COMMENT '关联拼团记录',
    `referred_by_member_id` bigint DEFAULT NULL COMMENT '推荐人ID',
    `referral_code_used` varchar(16) DEFAULT NULL COMMENT '推荐码',
    `real_name` varchar(50) NOT NULL COMMENT '真实姓名',
    `id_card` varchar(255) NOT NULL COMMENT '身份证号（加密）',
    `phone` varchar(20) NOT NULL COMMENT '手机号',
    `total_amount` decimal(10,2) NOT NULL COMMENT '应缴总金额',
    `deposit_amount_snapshot` decimal(10,2) NOT NULL COMMENT '定金快照',
    `balance_amount` decimal(10,2) NOT NULL COMMENT '尾款金额',
    `paid_amount` decimal(10,2) NOT NULL DEFAULT 0 COMMENT '已支付金额',
    `balance_due_date` date DEFAULT NULL COMMENT '尾款截止日期',
    `enrollment_status` varchar(30) NOT NULL DEFAULT 'PENDING_DEPOSIT' COMMENT '报名状态',
    `referral_right_granted` tinyint NOT NULL DEFAULT 0 COMMENT '是否已获得推荐权',
    `cancel_reason` varchar(500) DEFAULT NULL COMMENT '取消原因',
    `creator` varchar(64) DEFAULT '',
    `create_time` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP,
    `updater` varchar(64) DEFAULT '',
    `update_time` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP,
    `deleted` bit NOT NULL DEFAULT FALSE,
    `tenant_id` bigint NOT NULL DEFAULT 0,
    PRIMARY KEY (`id`)
) COMMENT '报名记录表';

CREATE TABLE IF NOT EXISTS `maritime_enrollment_order` (
    `id` bigint NOT NULL GENERATED BY DEFAULT AS IDENTITY COMMENT '订单ID',
    `enrollment_id` bigint NOT NULL COMMENT '报名ID',
    `order_no` varchar(64) NOT NULL COMMENT '订单号',
    `order_type` varchar(20) NOT NULL COMMENT '订单类型',
    `amount` decimal(10,2) NOT NULL COMMENT '应付金额',
    `pay_order_id` bigint DEFAULT NULL COMMENT 'pay模块订单ID',
    `pay_channel` varchar(20) DEFAULT NULL COMMENT '支付渠道',
    `order_status` varchar(20) NOT NULL DEFAULT 'PENDING' COMMENT '订单状态',
    `pay_time` datetime DEFAULT NULL COMMENT '支付时间',
    `expire_time` datetime DEFAULT NULL COMMENT '过期时间',
    `creator` varchar(64) DEFAULT '',
    `create_time` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP,
    `updater` varchar(64) DEFAULT '',
    `update_time` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP,
    `deleted` bit NOT NULL DEFAULT FALSE,
    `tenant_id` bigint NOT NULL DEFAULT 0,
    PRIMARY KEY (`id`)
) COMMENT '报名订单表';

CREATE TABLE IF NOT EXISTS `maritime_refund_apply` (
    `id` bigint NOT NULL GENERATED BY DEFAULT AS IDENTITY COMMENT '退费申请ID',
    `enrollment_id` bigint NOT NULL COMMENT '报名ID',
    `order_id` bigint DEFAULT NULL COMMENT '关联订单ID',
    `member_id` bigint NOT NULL COMMENT '申请人',
    `apply_reason` varchar(500) NOT NULL COMMENT '退费原因',
    `refund_amount` decimal(10,2) DEFAULT NULL COMMENT '退费金额',
    `apply_status` varchar(20) NOT NULL DEFAULT 'PENDING' COMMENT '申请状态',
    `admin_remark` varchar(500) DEFAULT NULL COMMENT '管理员备注',
    `approve_time` datetime DEFAULT NULL COMMENT '审核时间',
    `approver_id` bigint DEFAULT NULL COMMENT '审核人',
    `pay_refund_id` bigint DEFAULT NULL COMMENT 'pay_refund.id',
    `refund_time` datetime DEFAULT NULL COMMENT '退款到账时间',
    `creator` varchar(64) DEFAULT '',
    `create_time` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP,
    `updater` varchar(64) DEFAULT '',
    `update_time` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP,
    `deleted` bit NOT NULL DEFAULT FALSE,
    `tenant_id` bigint NOT NULL DEFAULT 0,
    PRIMARY KEY (`id`)
) COMMENT '退费申请表';

CREATE TABLE IF NOT EXISTS `maritime_groupon_record` (
    `id` bigint NOT NULL GENERATED BY DEFAULT AS IDENTITY COMMENT '拼团记录ID',
    `session_id` bigint NOT NULL COMMENT '班期ID',
    `invite_code` varchar(20) NOT NULL COMMENT '邀请码',
    `initiator_enrollment_id` bigint NOT NULL COMMENT '发起人报名ID',
    `required_count` int NOT NULL DEFAULT 3 COMMENT '需要人数',
    `current_count` int NOT NULL DEFAULT 1 COMMENT '当前人数',
    `groupon_status` varchar(20) NOT NULL DEFAULT 'IN_PROGRESS' COMMENT '拼团状态',
    `expire_time` datetime NOT NULL COMMENT '拼团截止时间',
    `success_time` datetime DEFAULT NULL COMMENT '成功时间',
    `creator` varchar(64) DEFAULT '',
    `create_time` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP,
    `updater` varchar(64) DEFAULT '',
    `update_time` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP,
    `deleted` bit NOT NULL DEFAULT FALSE,
    `tenant_id` bigint NOT NULL DEFAULT 0,
    PRIMARY KEY (`id`)
) COMMENT '拼团记录表';

CREATE TABLE IF NOT EXISTS `maritime_groupon_member` (
    `id` bigint NOT NULL GENERATED BY DEFAULT AS IDENTITY COMMENT '拼团成员ID',
    `groupon_record_id` bigint NOT NULL COMMENT '拼团记录ID',
    `enrollment_id` bigint NOT NULL COMMENT '报名ID',
    `member_id` bigint NOT NULL COMMENT '学员ID',
    `join_time` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '加入时间',
    `member_status` varchar(20) NOT NULL DEFAULT 'ACTIVE' COMMENT '成员状态',
    `deposit_paid_at` datetime DEFAULT NULL COMMENT '定金支付时间',
    `creator` varchar(64) DEFAULT '',
    `create_time` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP,
    `updater` varchar(64) DEFAULT '',
    `update_time` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP,
    `deleted` bit NOT NULL DEFAULT FALSE,
    `tenant_id` bigint NOT NULL DEFAULT 0,
    PRIMARY KEY (`id`)
) COMMENT '拼团成员表';

CREATE TABLE IF NOT EXISTS `maritime_referral_relation` (
    `id` bigint NOT NULL GENERATED BY DEFAULT AS IDENTITY COMMENT '推荐关系ID',
    `referrer_member_id` bigint NOT NULL COMMENT '推荐人',
    `referred_enrollment_id` bigint NOT NULL COMMENT '被推荐人报名ID',
    `referred_member_id` bigint NOT NULL COMMENT '被推荐人',
    `session_id` bigint NOT NULL COMMENT '班期ID',
    `relation_status` varchar(20) NOT NULL DEFAULT 'ACTIVE' COMMENT '关系状态',
    `invalid_reason` varchar(200) DEFAULT NULL COMMENT '无效原因',
    `first_click_at` datetime DEFAULT NULL COMMENT '首次点击时间',
    `referral_locked_at` datetime DEFAULT NULL COMMENT '锁定时间',
    `creator` varchar(64) DEFAULT '',
    `create_time` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP,
    `updater` varchar(64) DEFAULT '',
    `update_time` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP,
    `deleted` bit NOT NULL DEFAULT FALSE,
    `tenant_id` bigint NOT NULL DEFAULT 0,
    PRIMARY KEY (`id`)
) COMMENT '推荐关系表';

CREATE TABLE IF NOT EXISTS `maritime_commission_record` (
    `id` bigint NOT NULL GENERATED BY DEFAULT AS IDENTITY COMMENT '佣金记录ID',
    `referrer_member_id` bigint NOT NULL COMMENT '推荐人',
    `referred_enrollment_id` bigint NOT NULL COMMENT '被推荐人报名ID',
    `session_id` bigint NOT NULL COMMENT '班期ID',
    `commission_amount` decimal(10,2) NOT NULL COMMENT '佣金金额',
    `commission_status` varchar(30) NOT NULL DEFAULT 'WAITING_FOR_CLASS' COMMENT '状态',
    `expected_settle_date` date NOT NULL COMMENT '预计结算日期',
    `is_risk_flagged` tinyint NOT NULL DEFAULT 0 COMMENT '是否风控标记',
    `risk_check_result` text DEFAULT NULL COMMENT '风控详情',
    `trigger_time` datetime NOT NULL COMMENT '触发时间',
    `settle_check_time` datetime DEFAULT NULL COMMENT '结算检查时间',
    `approve_time` datetime DEFAULT NULL COMMENT '审核通过时间',
    `approver_id` bigint DEFAULT NULL COMMENT '审核人',
    `approve_remark` varchar(500) DEFAULT NULL COMMENT '审核备注',
    `payout_time` datetime DEFAULT NULL COMMENT '发放时间',
    `pay_transfer_id` bigint DEFAULT NULL COMMENT 'pay_transfer.id',
    `fail_reason` varchar(500) DEFAULT NULL COMMENT '失败原因',
    `retry_count` int NOT NULL DEFAULT 0 COMMENT '重试次数',
    `creator` varchar(64) DEFAULT '',
    `create_time` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP,
    `updater` varchar(64) DEFAULT '',
    `update_time` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP,
    `deleted` bit NOT NULL DEFAULT FALSE,
    `tenant_id` bigint NOT NULL DEFAULT 0,
    PRIMARY KEY (`id`)
) COMMENT '佣金记录表';

CREATE TABLE IF NOT EXISTS `maritime_commission_account` (
    `id` bigint NOT NULL GENERATED BY DEFAULT AS IDENTITY COMMENT '账户ID',
    `member_id` bigint NOT NULL COMMENT '推荐人ID',
    `total_amount` decimal(10,2) NOT NULL DEFAULT 0 COMMENT '累计佣金',
    `paid_amount` decimal(10,2) NOT NULL DEFAULT 0 COMMENT '已发放',
    `pending_amount` decimal(10,2) NOT NULL DEFAULT 0 COMMENT '待发放',
    `frozen_amount` decimal(10,2) NOT NULL DEFAULT 0 COMMENT '冻结中',
    `rejected_amount` decimal(10,2) NOT NULL DEFAULT 0 COMMENT '被拒绝',
    `total_referral_count` int NOT NULL DEFAULT 0 COMMENT '累计推荐人数',
    `version` int NOT NULL DEFAULT 0 COMMENT '乐观锁版本号',
    `creator` varchar(64) DEFAULT '',
    `create_time` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP,
    `updater` varchar(64) DEFAULT '',
    `update_time` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP,
    `deleted` bit NOT NULL DEFAULT FALSE,
    `tenant_id` bigint NOT NULL DEFAULT 0,
    PRIMARY KEY (`id`)
) COMMENT '推荐人佣金账户';

CREATE TABLE IF NOT EXISTS `maritime_risk_blacklist` (
    `id` bigint NOT NULL GENERATED BY DEFAULT AS IDENTITY COMMENT '黑名单ID',
    `phone` varchar(20) NOT NULL COMMENT '手机号',
    `reason` varchar(200) NOT NULL COMMENT '原因',
    `operator_id` bigint DEFAULT NULL COMMENT '操作人',
    `creator` varchar(64) DEFAULT '',
    `create_time` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP,
    `updater` varchar(64) DEFAULT '',
    `update_time` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP,
    `deleted` bit NOT NULL DEFAULT FALSE,
    `tenant_id` bigint NOT NULL DEFAULT 0,
    PRIMARY KEY (`id`)
) COMMENT '风控手机号黑名单';
