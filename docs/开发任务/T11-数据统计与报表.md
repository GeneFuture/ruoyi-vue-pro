# T11 - 数据统计与报表

> **阶段**：Phase 3 - Sprint 3.5  
> **周次**：第 13 周  
> **预估工时**：4 天  
> **前置条件**：[[T07-尾款支付与退费]] 完成（需要有完整业务数据）  
> **状态**：✅ 已完成

---

## 目标

实现管理后台 Dashboard（核心经营数据），以及常用 Excel 报表导出功能，支持财务对账。

---

## 1. Dashboard 首页数据

### 1.1 接口

```java
@GetMapping("/dashboard")
@Operation(summary = "管理后台 Dashboard 数据")
@PreAuthorize("@ss.hasPermission('maritime:dashboard:view')")
public CommonResult<AdminDashboardRespVO> getDashboard(
    @RequestParam(required = false) @DateTimeFormat(pattern = "yyyy-MM-dd")
    LocalDate date) { ... }
```

### 1.2 数据指标

**文件**：`AdminDashboardRespVO.java`

```java
public class AdminDashboardRespVO {
    // ── 今日数据 ──
    private Integer todayNewEnrollments;       // 今日新报名数
    private BigDecimal todayDepositAmount;      // 今日收款（定金）
    private BigDecimal todayBalanceAmount;      // 今日收款（尾款）
    
    // ── 本月数据 ──
    private Integer monthNewEnrollments;        // 本月新报名数
    private BigDecimal monthTotalRevenue;       // 本月总收款
    private BigDecimal monthCommissionPaid;     // 本月佣金发放总额
    private Integer monthSuccessGroupons;       // 本月成团数
    private Integer monthReferralCount;         // 本月推荐成功数
    
    // ── 班期招生进度（当前开放报名的班期）──
    private List<SessionProgressVO> openSessionProgress;
    
    // ── 趋势图（近7天/近30天报名量）──
    private List<DailyStatVO> enrollmentTrend;
    
    // ── 快速待办 ──
    private Integer pendingCommissionReviews;   // 待审核佣金数
    private Integer pendingRefundApplies;       // 待处理退费申请数
    private Integer failedPayouts;              // 发放失败佣金数
}

public class SessionProgressVO {
    private String sessionCode;
    private String courseName;
    private LocalDate startDate;
    private Integer maxStudents;
    private Integer enrolledCount;
    private Integer completedCount;      // 已完成（尾款全付）
    private BigDecimal confirmedRevenue; // 已确认收款
}
```

### 1.3 Service 实现

```java
public AdminDashboardRespVO getDashboard(LocalDate date) {
    LocalDate today = (date != null) ? date : LocalDate.now();
    LocalDate monthStart = today.withDayOfMonth(1);
    
    return new AdminDashboardRespVO()
        // 今日报名数（当天 00:00 到现在）
        .setTodayNewEnrollments(enrollmentMapper.countByCreateTimeBetween(
            today.atStartOfDay(), today.atTime(23, 59, 59)))
        // 今日收款
        .setTodayDepositAmount(enrollmentOrderMapper.sumPaidAmountByType(
            today.atStartOfDay(), today.atTime(23, 59, 59), OrderTypeEnum.DEPOSIT))
        // ... 其他统计（均为简单 COUNT/SUM 聚合查询）
        ;
}
```

> **性能提示**：Dashboard 查询较多，建议加 Redis 缓存（5分钟 TTL），避免每次访问都查数据库。

---

## 2. 班期报名名单导出

### 2.1 接口

```java
@GetMapping("/export-enrollment")
@Operation(summary = "导出班期报名名单（Excel）")
public void exportEnrollment(
    @RequestParam Long sessionId,
    HttpServletResponse response) throws IOException { ... }
```

### 2.2 导出字段

使用 ruoyi-vue-pro 内置的 EasyExcel 集成：

```java
@ExcelProperty("序号")        Integer index;
@ExcelProperty("姓名")        String realName;
@ExcelProperty("身份证号")    String idCard;      // 脱敏：前4后4，中间*
@ExcelProperty("手机号")      String phone;       // 脱敏：前3后4，中间****
@ExcelProperty("班期编号")    String sessionCode;
@ExcelProperty("报名时间")    String createTime;
@ExcelProperty("报名状态")    String status;      // 中文显示：待付定金/已完成/...
@ExcelProperty("定金金额")    BigDecimal depositAmount;
@ExcelProperty("尾款金额")    BigDecimal balanceAmount;
@ExcelProperty("是否推荐人")  String isReferrer;  // 是/否
@ExcelProperty("推荐人姓名")  String referrerName; // 脱敏
```

### 2.3 权限控制

- 导出包含身份证（脱敏）：需要 `maritime:enrollment:export` 权限
- 导出完整身份证：需要额外的 `maritime:enrollment:export-full` 权限（高权限）

---

## 3. 佣金发放明细导出

```java
@GetMapping("/export-commission")
@Operation(summary = "导出佣金发放明细（Excel）")
public void exportCommission(
    @RequestParam(required = false) String startDate,
    @RequestParam(required = false) String endDate,
    @RequestParam(required = false) String status,
    HttpServletResponse response) { ... }
```

导出字段：
- 序号、推荐人姓名（脱敏）、推荐人手机（脱敏）、被推荐人姓名（脱敏）
- 班期、佣金金额、状态、触发时间、发放时间、企业付款单号

---

## 4. 财务对账汇总

### 4.1 月度对账页面

```java
@GetMapping("/finance-summary")
@Operation(summary = "月度财务汇总")
public CommonResult<FinanceSummaryRespVO> getFinanceSummary(
    @RequestParam @DateTimeFormat(pattern = "yyyy-MM") YearMonth month) { ... }
```

返回：
```java
public class FinanceSummaryRespVO {
    private YearMonth month;
    
    // 收款
    private BigDecimal totalDeposit;           // 定金收款总额
    private BigDecimal totalBalance;           // 尾款收款总额
    private BigDecimal totalRevenue;           // 合计收款
    
    // 退款
    private BigDecimal totalRefund;            // 退款总额
    
    // 净收款
    private BigDecimal netRevenue;             // 净收款 = 总收款 - 退款
    
    // 佣金支出
    private BigDecimal totalCommissionPaid;    // 佣金发放总额
    private Integer commissionPaidCount;       // 发放笔数
    
    // 各班期明细
    private List<SessionRevenueSummaryVO> sessionDetails;
}
```

---

## 5. 推荐人排行榜（可选，Phase 2+）

```java
@GetMapping("/referral-leaderboard")
@Operation(summary = "推荐人排行榜")
public CommonResult<List<ReferralLeaderboardVO>> getLeaderboard(
    @RequestParam String period) { ... }  // WEEK/MONTH/ALL_TIME
```

- 使用 Redis ZSet：`maritime:leaderboard:month:2026-06` → member_id, score（推荐成功数）
- 每次推荐成功时 `ZINCRBY`
- 查询时 `ZREVRANGE ... WITHSCORES`

---

## 6. 接口清单

| 端 | Method | Path | 说明 |
|----|--------|------|------|
| 后台 | GET | `/admin-api/maritime/stats/dashboard` | Dashboard 汇总 |
| 后台 | GET | `/admin-api/maritime/stats/export-enrollment` | 报名名单导出 |
| 后台 | GET | `/admin-api/maritime/stats/export-commission` | 佣金明细导出 |
| 后台 | GET | `/admin-api/maritime/stats/finance-summary` | 月度财务汇总 |
| 后台 | GET | `/admin-api/maritime/stats/referral-leaderboard` | 推荐排行榜 |

---

## 7. 测试要点

- [ ] Dashboard 数据与数据库实际数据对比，确保计算准确
- [ ] Excel 导出文件可正常打开，字段无乱码
- [ ] 身份证、手机号导出时已脱敏
- [ ] 财务汇总：总收款 = 定金 + 尾款 - 退款（验证计算）
- [ ] 高并发查询时 Dashboard 有缓存保护（验证 Redis 缓存生效）

---

## 完成标准

- [ ] Dashboard 展示 4 类核心指标（今日/本月/班期进度/待办）
- [ ] 报名名单 Excel 正确导出
- [ ] 佣金明细 Excel 正确导出
- [ ] 月度财务汇总数据正确

---

## 完成后进入

→ [[T12-测试与上线]]
